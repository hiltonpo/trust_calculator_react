module.exports = {
  // 一行的字符數，如果超過會進行換行，默认為80
  printWidth: 120,
  // 一個tab代表幾個空格數，默认為2
  tabWidth: 2,
  // 是否使用tab進行縮排，默认為false，表示用空格進行縮減
  useTabs: false,
  // 字串是否使用單引號，默认為false，使用雙引號
  singleQuote: true,
  // 是否使用尾逗號，有三個可選值"<none|es5|all>"
  trailingComma: "all",
  // 物件大括號直接是否有空格，默认為true，效果：{ foo: bar }
  bracketSpacing: true,
  // Vue 檔案的 script 標籤和 Style 標籤下的內容需要縮進
  vueIndentScriptAndStyle: true,
  // 句尾添加分號
  semi: true,
};
